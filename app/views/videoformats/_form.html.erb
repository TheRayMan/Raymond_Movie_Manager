<%= form_for(@videoformat) do |f| %>
  <% if @videoformat.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@videoformat.errors.count, "error") %> prohibited this videoformat from being saved:</h2>

      <ul>
      <% @videoformat.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
